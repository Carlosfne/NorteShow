import React from 'react';
import { View, Text } from 'react-native';
import { WebView } from 'react-native-webview';

function ComoChegar() {
  return <Text>ComoChegar</Text>
}

export default ComoChegar;